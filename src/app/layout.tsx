import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Earnings Call Analyzer - Powered by Gatewayz AI",
  description: "Advanced NLP analysis for financial reports and earnings calls. Generate summaries, sentiment analysis, and interactive Q&A powered by Gatewayz AI API.",
  keywords: ["earnings call", "financial analysis", "sentiment analysis", "AI", "NLP", "Gatewayz"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
